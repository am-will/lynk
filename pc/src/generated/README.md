# Generated Sources

`codex-app-server/` is generated local output for inspecting the legacy Codex app-server schema.
It is intentionally gitignored. Regenerate it with:

```sh
cd pc
npm run codex:schemas
```

The runtime legacy Codex adapter is hand-written in `src/dispatcher/CodexAppServerClient.ts`.
